from django.contrib import admin

# Analytics app doesn't have models to register
# All analytics data is computed from other app models
# Admin interface for analytics would be handled through
# custom admin views or dashboard integration
